#ifndef TYPES_H
#define TYPES_H
#define BYTE_SIZE 8
#define WORD_SIZE 16
#define D_WORD_SIZE 32
#define Q_WORD_SIZE 64

#include <stdlib.h>

typedef uint8_t Byte;
typedef uint16_t Word;
typedef uint32_t D_Word;
typedef uint64_t Q_Word;

#endif